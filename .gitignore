# Heavy original videos — compressed web versions live in /videos and are committed.
# Originals stay local as backup, not pushed.
videos carousel/

# macOS
.DS_Store

# Editor / tooling
.claude/
.vscode/

# Logs & temp
*.log
scratchpad/

# Dev-only carousel tuning dashboard — never ship
tune.html

# Dependencies
node_modules/
