# Heavy original videos — compressed web versions live in /videos and are committed.
# Originals stay local as backup, not pushed.
videos carousel/

# macOS
.DS_Store

# Editor / tooling
.claude/
.vscode/

# Logs & temp
*.log
scratchpad/

# Dev-only tuning dashboards — never ship
tune.html
tune-dp.html

# Dependencies
node_modules/
